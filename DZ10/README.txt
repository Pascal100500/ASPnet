## DZ10 — Custom Tag Helpers (Razor Pages)

Учебный проект на ASP.NET Core Razor Pages, посвящённый созданию **пользовательских Tag Helper**.

## 🎯 Цель задания

- Закрепить работу с Razor Tag Helpers,
- Научиться создавать собственные HTML-теги,
- Реализовать серверную логику генерации HTML (с помощью Razor Tag Helpers без использования JavaScript).

---

## 🧩 Реализованные Tag Helpers

### 1️⃣ `<image>`

Пример использования:
```html
<image>sun</image>

Результат:
Вывод изображения солнца

🔹 Особенность реализации:
Tag Helper проверяет существование изображения в папке wwwroot/images.
Если файл существует → генерируется тег <img>
Если файл не найден → выводится текст
«Картинка не загрузилась»
Таким образом fallback-логика реализована на сервере, а не через JavaScript.

2️⃣ <time offset="N">

Пример использования:
<p>Москва: <time offset="3"></time></p>
<p>Лондон: <time offset="0"></time></p>
<p>Токио: <time offset="9"></time></p>

Результат:

Отображается текущее время.
Время рассчитывается относительно GMT с заданным смещением.
