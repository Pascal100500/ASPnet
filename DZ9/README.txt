## Проект: DZ9 — Game Portal (Razor Pages + Master Page)

Учебный проект на ASP.NET Core Razor Pages с использованием мастер-страницы (_Layout.cshtml).

Тематика проекта: **сайт с играми**.

---

## 📄 Страницы проекта

- **Home (Главная)** — описание портала и список игр
- **About (О разработчике)** — информация о разработчике

Все страницы используют общую мастер-страницу (Layout).

---

## 🧩 Реализовано

- Мастер-страница с:
  - шапкой сайта (header)
  - навигационным меню (nav)
  - основной областью контента (RenderBody)
  - подвалом сайта (footer)

- В подвале отображается:
  - текущий год (через `DateTime.Now.Year`)
  - версия сайта
```md
Пример:
```html
<p>© @DateTime.Now.Year Game Portal</p>
<p>Версия сайта: 1.0</p>

### Особенность оформления (CSS)

Изначально стили были заданы в файле `_Layout.cshtml.css`, 
который является scoped CSS (изолированным CSS для Razor Pages).

ASP.NET Core автоматически добавляет специальные атрибуты к HTML-элементам 
(например, `b-xxxx`), чтобы стили применялись только к конкретной странице.

Из-за этого селектор `nav a` не применялся к ссылкам,
созданным с помощью Tag Helpers (`asp-page`).

Для решения проблемы стили были перенесены в файл `site.css`,
который является глобальным CSS и корректно применяется ко всем элементам страницы.